{
  "name": "rekhers.github.io",
  "version": "0.1.0",
  "description": "A library of React components, utilities, and styles for The New Yorker Interactives team",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "parcel src/index.html --no-source-maps --no-cache --out-dir ./docs",
    "build": "parcel src/index.html --no-source-maps --no-cache --out-dir ./docs"
  },
  "dependencies": {
    "parcel-bundler": "^1.12.5"
  }
}
